#include <bits/stdc++.h>
using namespace std;
string s,s1,s2;
int dem=0;
int main()
{
  getline(cin,s);
  getline(cin,s1);
  getline(cin,s2);
  while (s.size(s1) != string::npos
         { 
           int k= s.find(s1);
           s.replace(k,s1.size(),s2);
         }
         cout << s;
         return 0;
         }
